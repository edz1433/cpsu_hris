<IfModule mod_rewrite.c>
     RewriteEngine On

     # Route every request into the public/ folder so the "/public" segment
     # never has to appear in the URL. The web server strips it for you here.
     RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
